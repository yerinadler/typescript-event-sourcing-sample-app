{
  "watch": ["src/**/*.ts"],
  "ext": "ts",
  "exec": "rm -rf dist && tsc --build --force && tsc-alias && concurrently --raw \"node -r dotenv/config dist/api/index.js\" \"node -r dotenv/config dist/subscribers/index.js\""  
}